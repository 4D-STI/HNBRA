export type UserPermissionType = {
    idPermission: number,
    idSubSession: number,
    nip: string,
    permission: number,
    status: boolean,
    createdAt: Date,
    updatedAt: Date,
    expireAt: Date
}
